# Citrine & Salt reservations

Flask restaurant reservation application. See [DEPLOYMENT.md](DEPLOYMENT.md) for production setup, environment variables, database guidance, and the Gunicorn startup command.

Run locally with `python app.py`; run tests with `pytest`.
